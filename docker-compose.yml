version: "3.9"
services:
  tp2intervals:
    image: ghcr.io/costa-alex/tp2intervals:latest
    container_name: tp2intervals
    restart: unless-stopped
    ports:
      - "8098:8080"
    volumes:
      - ./data:/data